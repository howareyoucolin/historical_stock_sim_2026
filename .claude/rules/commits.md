# Commit Rules

- Inspect staged changes only when preparing a commit.
- Do not auto-stage files as part of the commit workflow.
- Propose exactly 3 single-line commit message options in
  `(action): (message)` format.
- Wait for explicit user confirmation before running `git commit`.
